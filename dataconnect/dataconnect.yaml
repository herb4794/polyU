specVersion: "v1"
serviceId: "hosting"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hosting-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
